<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto">

  <data>
    <variable
      name="enabled"
      type="Boolean" />
  </data>

  <LinearLayout
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    android:background="@color/bg_app"
    android:orientation="vertical">

    <ImageView
      android:id="@+id/iv_ic_select_category"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:padding="20dp"
      android:background="@drawable/bg_select_category_item_disabled"
      app:tint="@color/select_category_ic_color"
      android:src="@drawable/ic_sc_phone_iphone_24" />


    <TextView
      android:id="@+id/tv_ic_select_category"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:fontFamily="@font/markpro_medium"
      android:text="Phones"
      android:textColor="@color/categoryTitleText"
      android:textSize="12sp" />


  </LinearLayout>
</layout>

